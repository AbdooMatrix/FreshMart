name: freshmart
description: FreshMart — Online Grocery Store
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.2.0                    # API calls to FastAPI
  google_fonts: ^8.1.0            # Epilogue + Work Sans
  cached_network_image: ^3.3.1    # Smart image loading + fallback
  intl: ^0.20.2                   # Currency formatting

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  # No asset folders needed — all images come from API URLs